import React from 'react';
import FolderList from '../folder-list/FolderList';

const MainWrapper = () => {
  return (
    <div>
      <FolderList folders={[]} />
    </div>
  );
};

export default MainWrapper;
